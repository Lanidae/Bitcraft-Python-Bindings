# AUTO-GENERATED by generate_bindings.py — DO NOT EDIT MANUALLY.
# Re-run generate_bindings.py to update from the latest bindings graph.
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional

from enum import IntEnum


class EnemyStatus(IntEnum):
    INACTIVE = 0
    IDLE = 1
    RETURNINGTOIDLE = 2
    EVASIVE = 3
    INVESTIGATING = 4
    FIGHTING = 5
    RETREATING = 6


    @classmethod
    def from_row(cls, raw) -> 'EnemyStatus':
        """
        Raw wire format: [variant_index, payload]
        e.g. [0, []] for Inactive
        """
        if isinstance(raw, list):
            return cls(raw[0])
        if isinstance(raw, int):
            return cls(raw)
        raise ValueError(f'Cannot parse EnemyStatus from {raw!r}')
