# AUTO-GENERATED by generate_bindings.py — DO NOT EDIT MANUALLY.
# Re-run generate_bindings.py to update from the latest bindings graph.
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional



@dataclass
class ResourceClumpDesc:
    id: int = 0
    resource_id: list = field(default_factory=list)
    x: list = field(default_factory=list)
    z: list = field(default_factory=list)
    direction: list = field(default_factory=list)

    @classmethod
    def from_row(cls, raw) -> 'ResourceClumpDesc':
        if raw is None:
            return cls()
        return cls(
            id=(raw.get('id') if isinstance(raw, dict) else raw[0]),
            resource_id=[_item for _item in ((raw.get('resource_id') if isinstance(raw, dict) else raw[1]) or [])],
            x=[_item for _item in ((raw.get('x') if isinstance(raw, dict) else raw[2]) or [])],
            z=[_item for _item in ((raw.get('z') if isinstance(raw, dict) else raw[3]) or [])],
            direction=[_item for _item in ((raw.get('direction') if isinstance(raw, dict) else raw[4]) or [])]
        )
