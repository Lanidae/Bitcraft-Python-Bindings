# AUTO-GENERATED by generate_bindings.py — DO NOT EDIT MANUALLY.
# Re-run generate_bindings.py to update from the latest bindings graph.
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional

from enum import IntEnum


class ProgressiveActionStatus(IntEnum):
    NONE = 0
    ACTIVE = 1
    SUSPENDED = 2
    COMPLETED = 3


    @classmethod
    def from_row(cls, raw) -> 'ProgressiveActionStatus':
        """
        Raw wire format: [variant_index, payload]
        e.g. [0, []] for None
        """
        if isinstance(raw, list):
            return cls(raw[0])
        if isinstance(raw, int):
            return cls(raw)
        raise ValueError(f'Cannot parse ProgressiveActionStatus from {raw!r}')
