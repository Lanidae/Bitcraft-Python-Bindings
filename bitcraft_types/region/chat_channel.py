# AUTO-GENERATED by generate_bindings.py — DO NOT EDIT MANUALLY.
# Re-run generate_bindings.py to update from the latest bindings graph.
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional

from enum import IntEnum


class ChatChannel(IntEnum):
    SYSTEM = 0
    GLOBAL = 1
    LOCAL = 2
    REGION = 3
    CLAIM = 4
    EMPIREPUBLIC = 5
    EMPIREINTERNAL = 6
    LOOKINGFORGROUP = 7
    TRADE = 8


    @classmethod
    def from_row(cls, raw) -> 'ChatChannel':
        """
        Raw wire format: [variant_index, payload]
        e.g. [0, []] for System
        """
        if isinstance(raw, list):
            return cls(raw[0])
        if isinstance(raw, int):
            return cls(raw)
        raise ValueError(f'Cannot parse ChatChannel from {raw!r}')
