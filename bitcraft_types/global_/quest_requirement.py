# AUTO-GENERATED by generate_bindings.py — DO NOT EDIT MANUALLY.
# Re-run generate_bindings.py to update from the latest bindings graph.
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional

from enum import IntEnum


class QuestRequirement(IntEnum):
    PADDINGNONE = 0
    QUESTCHAIN = 1
    ACHIEVEMENT = 2
    COLLECTIBLE = 3
    LEVEL = 4
    ITEMSTACK = 5
    SECONDARYKNOWLEDGE = 6


    @classmethod
    def from_row(cls, raw) -> 'QuestRequirement':
        """
        Raw wire format: [variant_index, payload]
        e.g. [0, []] for PaddingNone
        """
        if isinstance(raw, list):
            return cls(raw[0])
        if isinstance(raw, int):
            return cls(raw)
        raise ValueError(f'Cannot parse QuestRequirement from {raw!r}')
