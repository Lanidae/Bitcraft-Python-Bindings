# AUTO-GENERATED by generate_bindings.py — DO NOT EDIT MANUALLY.
# Re-run generate_bindings.py to update from the latest bindings graph.
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional

from enum import IntEnum


class NpcType(IntEnum):
    NONE = 0
    RUMBAGH = 1
    SVIM = 2
    HEIMLICH = 3
    TWINS = 4
    BRICO = 5
    TAMER = 6
    SLAYER = 7


    @classmethod
    def from_row(cls, raw) -> 'NpcType':
        """
        Raw wire format: [variant_index, payload]
        e.g. [0, []] for None
        """
        if isinstance(raw, list):
            return cls(raw[0])
        if isinstance(raw, int):
            return cls(raw)
        raise ValueError(f'Cannot parse NpcType from {raw!r}')
